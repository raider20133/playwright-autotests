# Frontend under test
BASE_URL=https://managmenttool-front-end.onrender.com
# Backend API
API_BASE_URL=https://managmenttool.onrender.com
# Registration code — the only secret the suite needs: every test registers its own user with it
SECRET_PASSWORD=
