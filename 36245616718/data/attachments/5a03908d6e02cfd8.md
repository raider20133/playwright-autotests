# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/ui/tasks.spec.ts >> Checklist >> completing a task strikes it through and saves the status
- Location: tests/ui/tasks.spec.ts:45:5

# Error details

```
TimeoutError: apiRequestContext.fetch: Timeout 30000ms exceeded.
Call log:
  - → POST https://managmenttool.onrender.com/api/register
    - user-agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/153.0.8010.12 Safari/537.36
    - accept: */*
    - accept-encoding: gzip,deflate,br
    - content-type: application/json
    - content-length: 100

```

# Test source

```ts
  1  | import type {APIRequestContext} from '@playwright/test';
  2  | 
  3  | export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';
  4  | 
  5  | export interface ApiResponse {
  6  |     method: HttpMethod;
  7  |     url: string;
  8  |     status: number;
  9  |     body: unknown;
  10 | }
  11 | 
  12 | export interface SendOptions {
  13 |     data?: unknown;
  14 |     params?: Record<string, string | number | boolean>;
  15 |     /** Override the client's token: a string sends it as-is, null sends no Authorization header. */
  16 |     token?: string | null;
  17 | }
  18 | 
  19 | /**
  20 |  * Thin wrapper over APIRequestContext. It never throws on HTTP errors — tests assert
  21 |  * the status explicitly, so negative cases read the same way as positive ones.
  22 |  */
  23 | export class BaseApi {
  24 |     constructor(
  25 |         protected readonly http: APIRequestContext,
  26 |         private readonly token?: string,
  27 |     ) {}
  28 | 
  29 |     protected async send(method: HttpMethod, path: string, options: SendOptions = {}): Promise<ApiResponse> {
  30 |         const token = options.token === undefined ? this.token : options.token;
> 31 |         const response = await this.http.fetch(path, {
     |                                          ^ TimeoutError: apiRequestContext.fetch: Timeout 30000ms exceeded.
  32 |             method,
  33 |             data: options.data,
  34 |             params: options.params,
  35 |             headers: token ? {Authorization: `Bearer ${token}`} : {},
  36 |             failOnStatusCode: false,
  37 |         });
  38 |         const text = await response.text();
  39 |         let body: unknown = text || null;
  40 |         try {
  41 |             body = text ? JSON.parse(text) : null;
  42 |         } catch {
  43 |             // Non-JSON bodies (e.g. "Forbidden") stay as text
  44 |         }
  45 |         return {method, url: response.url(), status: response.status(), body};
  46 |     }
  47 | }
  48 | 
```