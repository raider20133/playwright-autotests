{
  "name": "playwrigth",
  "version": "1.0.0",
  "description": "Run all autotests: npx playwright test Run one autotest:  npx playwright test tests/integration/leaveRequests/leaveRequests.spec.ts Run test inside autotests: npx playwright test -g \"addToCart\"",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
